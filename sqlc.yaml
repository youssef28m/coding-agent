# sqlc.yaml (project root)
version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/db/query.sql"
    schema: "migrations"
    gen:
      go:
        package: "sqlcgen"
        out: "internal/db/sqlcgen"
        emit_json_tags: true
        emit_interface: true